# @rnboost/cli

The smart React Native starter — scaffold a production-ready app in one command.

```bash
npx rnboost init myApp
```

See the [project root README](../../README.md) for the full feature list.
